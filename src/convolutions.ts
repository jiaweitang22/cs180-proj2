export const fourLoopCode = "def conv2d_loops4(image: np.ndarray, kernel: np.ndarray, mode: str = \"same\") -> np.ndarray:\n    '''2D convolution with four nested loops and zero padding.\n\n    mode: \"same\" or \"full\". The kernel is flipped for true convolution.\n    '''\n    if mode not in {\"same\", \"full\"}:\n        raise ValueError(f\"mode must be 'same' or 'full', got {mode!r}\")\n\n    h, w = image.shape\n    kh, kw = kernel.shape\n    flipped_kernel = np.flip(kernel)\n\n    if mode == \"same\":\n        top, bottom = kh // 2, (kh - 1) // 2\n        left, right = kw // 2, (kw - 1) // 2\n        out_h, out_w = h, w\n    else:  # mode == \"full\"\n        top = bottom = kh - 1\n        left = right = kw - 1\n        out_h, out_w = h + kh - 1, w + kw - 1\n\n    padded = np.pad(\n        image,\n        ((top, bottom), (left, right)),\n        mode=\"constant\",\n        constant_values=0,\n    )\n    result = np.zeros((out_h, out_w), dtype=np.float64)\n\n    for out_r in range(out_h):\n        for out_c in range(out_w):\n            total = 0.0\n            for kernel_r in range(kh):\n                for kernel_c in range(kw):\n                    total += (\n                        padded[out_r + kernel_r, out_c + kernel_c]\n                        * flipped_kernel[kernel_r, kernel_c]\n                    )\n            result[out_r, out_c] = total\n\n    return result";

export const twoLoopCode = "def conv2d_loops2(image: np.ndarray, kernel: np.ndarray, mode: str = \"same\") -> np.ndarray:\n    '''2D convolution with two nested loops and zero padding.\n\n    mode: \"same\" or \"full\". The kernel is flipped for true convolution.\n    '''\n    if mode not in {\"same\", \"full\"}:\n        raise ValueError(f\"mode must be 'same' or 'full', got {mode!r}\")\n\n    h, w = image.shape\n    kh, kw = kernel.shape\n    flipped_kernel = np.flip(kernel)\n\n    if mode == \"same\":\n        top, bottom = kh // 2, (kh - 1) // 2\n        left, right = kw // 2, (kw - 1) // 2\n        out_h, out_w = h, w\n    else:  # mode == \"full\"\n        top = bottom = kh - 1\n        left = right = kw - 1\n        out_h, out_w = h + kh - 1, w + kw - 1\n\n    padded = np.pad(\n        image,\n        ((top, bottom), (left, right)),\n        mode=\"constant\",\n        constant_values=0,\n    )\n    result = np.zeros((out_h, out_w), dtype=np.float64)\n\n    for out_r in range(out_h):\n        for out_c in range(out_w):\n            patch = padded[out_r:out_r + kh, out_c:out_c + kw]\n            result[out_r, out_c] = np.sum(patch * flipped_kernel)\n    return result";
